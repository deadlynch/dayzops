[project]
name = "dayzops"
version = "0.1.0"
description = "Declarative DayZ server operations platform"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "PyYAML>=6.0"
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"